# How to publish
  - make your changes
  - commit your changes
  - run ```yarn build```
  - run ```npm version patch``` or ```npm version minor``` or ```npm version major``` depending on the change
  - run ```npm publish```
